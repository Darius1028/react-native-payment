import React from 'react';

import PaymentScreen from 'src/containers/PaymentsSreen';

const defaultProps = {};

const Payment = (props) => {
	return (
		<PaymentScreen />
	);
};

Payment.defaultProps = defaultProps;

export default Payment;
